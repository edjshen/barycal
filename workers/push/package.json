{
  "name": "barycal-push",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Barycal push-reminder scheduler — a cron-only Cloudflare Worker that pings the app's /api/cron/reminders route to send daily push digests.",
  "scripts": {
    "dev": "wrangler dev -c ./wrangler.toml",
    "deploy": "wrangler deploy -c ./wrangler.toml",
    "tail": "wrangler tail -c ./wrangler.toml",
    "test:scheduled": "wrangler dev -c ./wrangler.toml --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
